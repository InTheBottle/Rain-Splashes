[Splashes]

; Splash size multiplier. Each splash still varies a little on its own.
; Range 0.1 - 5.0, default 1.0
Scale = 1.0

; How many NPCs can splash at the same time. Lower this if crowded cities cost
; you frames. The player is never counted and never dropped.
; Range 0 - 200, default 12
MaxNPCs = 12